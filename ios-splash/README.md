# iOS Splash Screen Assets Needed

Create splash screen images with the VASA logo centered on #0a0a0a background.

Recommended approach: Use a single 2732x2732 PNG image and let Capacitor handle scaling.

Place the image as: splash.png

The splash screen will display for 2 seconds while the app loads.
